using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NetworkClientUseCase : MonoBehaviour
{
    [SerializeField] private TortecClientUseCase _tortecClient;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
